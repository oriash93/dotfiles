#!/bin/bash

REPO_URL=`git ls-remote --get-url origin | awk -F '@' '{print $2}' | awk -F '.git' '{print $1}'`
BRANCH_NAME=`git branch --show-current`

URL=$REPO_URL/branch/$BRANCH_NAME
echo Opening $URL ...

start chrome $URL
